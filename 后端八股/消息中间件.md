消息中间件是一种软件基础设施，设置在生产者与消费者之间，用在分布系统中的不同应用程序或服务之间的异步传递消息
- **系统解耦**
- **异步处理**
- **削峰填谷**

## RabbitMQ
##### 1. RabbitMQ 如何保证消息不丢失
在每一个环节都有可能导致消息丢失
![[Pasted image 20260419193935.png]]
###### 1.1 生产者确认机制
RabbitMQ提供了publisher confirm机制来避免消息发送到MQ过程中丢失。==消息发送到MQ以后，会返回一个结果给发送者，表示消息是否处理成功==
![[截屏2026-04-21 15.08.33.png]]
- 消息失败之后如何处理？
	- 回调方法即时重发
	- 记录日志
	- 保存到数据库后然后定时重发，成功发送后即刻删除表中的数据
###### 1.2 消息持久化
==MQ默认是内存存储消息，开启持久化功能，即写入硬盘==，可以确保缓存在MQ中的消息不丢失
- **交换机持久化**
- **队列持久化**
- **消息持久化**
	SpringAMQ中的消息默认是持久的，可以通过MessageProperties中的DeliveryMode来指定
###### 1.3 消费者确认
RabbitMQ支持消费者确认机制，即：==消费者处理消息后可以向MQ发送ack回执==，MQ收到ack回执后才会删除改消息，而SpringAMQP则允许配置三种确认模式：
1. `manual`：手动ack，需要在业务代码结束后，调用api发送ack
2. `auto`：自动ack，由spring检测listener代码是否出现异常，没有异常则返回ack，抛出异常则返回nack
3. `none`：关闭ack，MQ嘉定消费者获取消息后会成功处理，因此消息投递后立刻被删除
我们可以利用Spring的retry机制，在消费者出现异常时利用本地重试，设置重试次数，当次数达到了之后，如果消息依然失效，将消息投递到异常交换机，交由人工处理
![[截屏2026-04-21 15.38.06.png]]
> [!NOTE] 什么是ack
> ack：acknowledgement，确认消息
###### 1.4 面试回答
- 开启生产者确认机制，确认生产者的消息能到达队列
- 开启持久化功能，确保消息为消费前在队列中不会丢失
- 开启消费者确认机制为auto，由spring确认消息处理成功后完成ack
- 开启消费者失败重试机制，多次重试失败后将消息投递到异常交换机，交由人工处理
##### 2. RabbitMQ消息的重复消费问题如何解决
比如网络抖动或者消费者挂了
- 解决方案：
	- 每个消息设置一个唯一的标识id
	- 幂等方案：分布式锁，数据库锁（悲观锁，乐观锁）
![[截屏2026-04-21 15.44.58.png]]
##### 3. RabbitMQ中死信交换机（RabbitMQ延迟队列）
- 延迟队列：进入队列的消息会被延迟消费的队列
- 场景：超时订单，限时优惠，定时发布
。。。。
##### 4. RabbitMQ如果有100万消息堆积在MQ，如何解决（消息堆积如何解决）
当生产者发送消息的速度超过了消费者处理消息的速度，就会导致队列中的消息堆积，直到队列存储消息达到上限，之后发送的消息就会成为死信，可能会被丢弃，这就是消息堆积问题
- 解决方案：
	1. 增加更多消费者，提高消费速度
	2. 在消费者内开启线程池加快消息处理速度
	3. 扩大队列容积，提高堆积上限，采用惰性队列
###### 4.1 惰性队列
惰性队列的特征是：
1. 接收到消息后直接存入磁盘而非内存
2. 消费者要消费消息时才会从磁盘中读取并加载到内存当中
3. 支持数百万条的消息存储
###### 4.2 面试回答
- 增加更多消费者，提高消费速度  
- 在消费者内开启线程池加快消息处理速度  
- 扩大队列容量，提高堆积上限，采用惰性队列
	- 在声明队列的时候可以设置属性 `x-queue-mode` 为 `lazy`，即为惰性队列  
	- 基于磁盘存储，消息上限高  
	- 性能比较稳定，但基于磁盘存储，受限于磁盘 IO，时效性会降低
##### 5. RabbitMQ高可用机制
- 在生产环境下，使用集群来保证高可用性
> [!NOTE] 集群
> 集群（Cluster）是由多台计算机（节点）通过网络连接组成的系统，这些节点协同工作，对外提供统一服务，并在逻辑上表现为一个整体
###### 5.1 普通集群
普通集群，又叫标准集群，具备以下特征：
- 会在集群的各个节点间共享部分数据，包括：交换机、队列元信息。不包含队列中的消息
- 当访问集群某节点时，如果队列不在该节点，会从数据所在节点传递到当前节点并返回
	- 如果你连接的是B机器，但是消息在A机器上，那就会发生 你 → B → A → B → 你，即B会帮你去A拿消息，再转给你
- 队列所在节点宕机，队列中的消息就会丢失
###### 5.2 镜像集群
###### 5.3 镜像集群