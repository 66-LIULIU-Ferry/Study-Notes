## 计算机网络系结构
##### OSI七层模型
1. **应用层**：
2. **表示层**
3. **会话层**
4. **传输层**
5. **网络层**
6. **数据链路层**
7. **物理层**
##### TCP/IP四层模型

##### 五层结构

## 网络综合
##### 浏览器地址输入url到显示主页过程

##### DNS解析过程

## IP
##### IP协议作用

##### APP协作工作过程

## HTTP
###### GET和POST的区别

##### HTTP报文结构
##### HTTP工作流程

##### Session和Cookie
##### 状态码

## UDP
##### TCP和UDP的区别
##### UDP如何保证消息不丢失

## TCP





TCP

HTTP/HTTPS
服务端渲染是由服务器生成完整HTML返回给客户端，首屏加载快且利于SEO；客户端渲染是由浏览器通过JavaScript生成页面，交互性更强但首屏较慢

http method / status code
- 输入URL到页面展示过程
- TCP三次握手
- TCP vs UDP
- HTTP vs HTTPS
	- HTTPS 是在 HTTP 基础上加入 SSL/TLS 加密层，能够保证数据传输的安全性和完整性，防止被窃听和篡改。
- HTTP版本区别
	- HTTP1.0：短连接  
	- HTTP1.1：长连接  
	- HTTP2：多路复用  
	- HTTP3：无阻塞

- 服务端和客户端渲染的区别
服务端渲染是由服务器生成完整HTML返回给客户端，首屏加载快且利于SEO；客户端渲染是由浏览器通过JavaScript生成页面，交互性更强但首屏较慢。

```java
import java.io.*;  
  
BufferedReader br = new BufferedReader(new InputStreamReader(System.in));  
  
String line = br.readLine();
```