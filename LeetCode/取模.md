先在function里面定义 `static int MOD`
- 加法：`ans = (a + b) % MOD`
- **减法**：`and = (a - b + MOD) % MOD`
- 乘法：`ans = (a * b) % MOD`
- **除法**：`ans = (a * pow(b, MOD - 2, MOD)) % MOD`

> [!NOTE] 注意
> 在有MOD取模操作的题目里，尽量都去使用 long 作为数据类型，因为取模前的中间计算结果可能会超过int范围
